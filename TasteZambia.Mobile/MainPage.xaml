<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TasteZambia.Mobile.MainPage">

    <!-- Temporary probe for Task 1: proves the design tokens resolve and the
         three font families render. Replaced by HomePage in Task 7. -->
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <Label Style="{StaticResource KickerLabel}" Text="Living cultural archive"/>
            <Label Style="{StaticResource DisplayTitle}" Text="Discover the Taste of Zambia"/>
            <Label Style="{StaticResource SectionTitle}" Text="Food Categories"/>
            <Label Style="{StaticResource CardTitle}" Text="Ifisashi"/>
            <Label Style="{StaticResource EnglishSubtitle}" Text="Groundnut and greens relish"/>
            <Label Style="{StaticResource BodyText}"
                   Text="Leafy greens simmered in pounded groundnuts until the sauce thickens and the oil rises."/>
            <Label Style="{StaticResource MonoMeta}" Text="45 min  ·  Easy"/>

            <Border Style="{StaticResource PrimaryAction}">
                <Label Text="Explore Zambian Food" TextColor="{StaticResource TzSurface}"
                       FontFamily="ArchivoSemiBold" FontSize="13.5" HorizontalOptions="Center"/>
            </Border>

            <Border Style="{StaticResource CreamPanel}">
                <Label Text="Cream panel · TzCream" Style="{StaticResource BodySmall}"/>
            </Border>

            <Border Style="{StaticResource DeepPanel}">
                <Label Text="Deep panel · TzGreenDeep" FontFamily="NewsreaderMedium"
                       FontSize="21" TextColor="{StaticResource TzSurface}"/>
            </Border>

            <Image Source="ifisashi.png" HeightRequest="158" Aspect="AspectFill"/>
            <Image Source="logo_pot.png" HeightRequest="52" Aspect="AspectFit"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
